#include <bits/stdc++.h>
using namespace std;

int main() {

  int n;
  cin >> n;

  int total = 2*n+2;

  vector<vector<bool> >grid (total, vector<bool>(total, false));
  int x = n; 
  int y = n;

  for (int i = 0; i < n; i++) {
    char c;
    cin >> c;

    if (c== 'N') {
      
    }
  }

  
  return 0;
}